sudo: false

branches:
  only:
    - master

addons:
  apt:
    packages:
      - cmake
      - doxygen

script:
  - mkdir build-release
  - cd build-release
  - cmake ..
  - cat /home/travis/build/discopt/cmr/build-release/CMakeFiles/CMakeOutput.log
  - make doc

deploy:
  provider: pages
  skip_cleanup: true
  local_dir: build-release/doc/html
  github_token: $GH_REPO_TOKEN
  on:
    branch: master
