=== INSTALLATION ===

1. Create a build directory:

mkdir build

2. Go to the build directory:

cd build/

3. Configure the build:

cmake ..

4. Compile:

make

5a. Use one of binaries:

./cmr-totally-unimodular
./cmr-graphic
./cmr-series-parallel

5b. Install the binary and the library:

sudo make install

